/**
 * Shared helpers used by pages.
 *
 * - player.js  → remembers the player in the browser (localStorage)
 * - server/    → talks to Supabase (only runs on the server, never in the browser)
 *
 * Tip: import with `$lib/...`
 *   import { hasPlayedToday } from '$lib/player.js';
 */
